------ BLOQUES - ERRORES

procedure Hello is
    a: Integer;
    b: Float := 2.0 ;
    c: Boolean := false; 

    function uno(a,b:out Boolean; x,y :in Integer) return Boolean is

        begin
            if x < 19 or x > 89 then
                return False;
            else 
                return a;
            end if; 

    end uno;


    begin

        loop
            Get(b);
            exit when b < 3.0);
        end loop;

        while a < 3 loop
            Put(a);
        end loop;

        for J in a     
            if (J) /= 0
                Put(J));
            end if;
        end loop;

end Hello;